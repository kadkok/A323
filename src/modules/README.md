# Modules

A Fase 0 cria apenas a fundação modular. Cada módulo de negócio será adicionado como pacote interno em `src/modules/<module>` com manifesto, contratos públicos, service, repository, validators, UI, testes e documentação curta.
